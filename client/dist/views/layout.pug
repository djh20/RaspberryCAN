doctype html
html(lang="en")
  - var titleText = config.name || "RaspberryCAN"

  head
    title #{titleText}
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    script(src='/bundle.js')
  body
    include components/banner.pug
    include components/navigation.pug

    block banner
    block navigation

    block content