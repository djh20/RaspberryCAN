div.banner
  div#menu-button
    img.icon(src="icons/menu.svg")
  div.main
    h1.title #{titleText}
    div.status
      img.icon(id="status-icon" src="icons/tick.svg")
      span.label(id="status-label") Connecting...
    